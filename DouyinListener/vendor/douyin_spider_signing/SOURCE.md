# Source

These compatibility modules were adapted from the user-provided archive
`DouyinListener/DouYin_Spider-master.zip`, repository `cv-cat/DouYin_Spider`.

Only the pure Python browser fingerprint, SM3, `a_bogus`, and `strData`
implementations are included. The crawler, private-message client, GUI, and
automation code are intentionally excluded.
